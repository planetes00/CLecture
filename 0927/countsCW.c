#include <stdio.h>
int main()
{
    char text[256];//선언한다. 스트링 480정도면 되겠지. 뭐.
    int i=0, noChar=0;//캐릭터 개수, 단어개수, 
    printf("input a textline: ");
    scanf("%s",text);
    //입력해 주세요!!
    //받은거의 캐릭터를 세본다.
    //무한반복한다.
        //텍스트에 null나올때까지 세어보자


    //알아낸걸 알려준다.
    printf(""\"%s|" has %d characters\n",text noChar)
    return 0;
}